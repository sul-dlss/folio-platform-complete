server {
  listen 80;
  server_name localhost;
  error_page 403 tenant-assets/403.html
  charset utf-8;
  # Serve index.html for any request not found
  location / {
    # Set path
    root /usr/share/nginx/html;
    include mime.types;
    types {
      text/plain lock;
    }
    try_files $uri /index.html;
  }
}
